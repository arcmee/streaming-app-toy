# Web/Mobile client configuration
NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_SOCKET_SERVER_URL=http://localhost:4000

# Streaming server base (재생용 HTTP, 예: FLV/HLS). 기본 Node-Media-Server nginx-rtmp 예시: 8000 포트.
# OBS 인제스트(송출)는 RTMP 주소로 별도 입력: rtmp://localhost:1935/live
# 원격 서버일 경우 http://your-server.com:8000 과 rtmp://your-server.com:1935/live 형태로 맞춰주세요.
NEXT_PUBLIC_STREAMING_SERVER_URL=http://localhost:8000

# 서버(API)에서 사용하는 값들은 서버 경로(.env)에서 관리하세요.
# 예) DATABASE_URL, REDIS_URL, JWT_SECRET, JWT_EXPIRES_IN, JWT_ISSUER 등
